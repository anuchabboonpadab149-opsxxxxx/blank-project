{
  "name": "bigo-agent-topup",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal agent-based top-up system (no payments), form + API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}