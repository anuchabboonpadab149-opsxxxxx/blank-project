# Omise API keys (use live keys in production)
OMISE_PUBLIC_KEY=pkey_test_xxxxxxxxxxxxxxxxx
OMISE_SECRET_KEY=skey_test_xxxxxxxxxxxxxxxxx

# Webhook verification secret (used to verify payload signatures or header/query)
WEBHOOK_SECRET=change_this_to_a_random_secret

# PromptPay destination phone number (for display purposes only)
PROMPTPAY_PHONE=0916974995

# JWT secret for user tokens
JWT_SECRET=change_this_jwt_secret

# Server listening port
PORT=3000

# Allowed CORS origin (your frontend URL), e.g., https://your-frontend.com
CORS_ORIGIN=http://localhost:5173