# Omise API keys (use test keys for sandbox, live keys for production)
OMISE_SECRET_KEY=skey_test_xxxxxxxxxxxxxxxxxxxxxxxxx
OMISE_PUBLIC_KEY=pkey_test_xxxxxxxxxxxxxxxxxxxxxxxxx

# Webhook secret (set the same value in Omise dashboard for webhook signing)
WEBHOOK_SECRET=your_webhook_secret

# Server port
PORT=3000